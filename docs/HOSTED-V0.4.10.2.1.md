# v0.4.10.2.1 — Character Load Hotfix

Fixes the character-load `Cannot read properties of null (reading querySelector)` error introduced by v0.4.10.2.

The Essence-granted Training badge code now supports the compact Training row DOM and safely skips missing rows. The Hosted Player version label is also corrected.
