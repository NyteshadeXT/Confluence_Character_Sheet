# v0.4.9.1

No PostgreSQL schema migration is required.

Permanent milestone selections use the existing character `essence_choices_json`
storage. Rich choice definitions live inside `essence_definitions.definition`.
