# v0.4.9.2

No PostgreSQL migration is required.

The new milestone effects are stored inside existing Essence definition JSON. Combat
card state is transient UI state. Power formatting uses existing Power text fields.
